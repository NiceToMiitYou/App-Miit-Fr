( function() {

    ITEventApp.controller(
        'loginController',
        function( $scope ) {
            $scope.user = {
                mail: '',
                password: ''
            };

            $scope.next = function() {
                
            }

            $scope.previous = function() {
                
            }

        } );

} )();
